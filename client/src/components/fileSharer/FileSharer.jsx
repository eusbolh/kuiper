import React, { Component } from 'react';
import PropTypes from 'prop-types';

const FileSharer = ({ ...props }) => {
  return (
    <div className="k-file-sharer">
      File sharer
    </div>
  )
}

FileSharer.propTypes = {
};

FileSharer.defaultProps = {
};

export default FileSharer;
