import React, { Component } from 'react';
import PropTypes from 'prop-types';

const ConnectToClient = ({ ...props }) => {
  return (
    <div className="k-connect-to-client">
      Connect to client
    </div>
  )
}

ConnectToClient.propTypes = {
};

ConnectToClient.defaultProps = {
};

export default ConnectToClient;
